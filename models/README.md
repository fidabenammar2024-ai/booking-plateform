## Models

This directory contains the data model classes representing the core entities of the application:
- `User` — represents application users
- `Field` — represents the fields/venues available for booking
- `Reservation` — represents booking/reservation records