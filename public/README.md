## Public

This directory (often the web root) contains the entry-point files that are directly accessible via the browser, including:
- `index.php` — main landing page
- `login.php` — user login page
- `register.php` — user registration page
- `dashboard.php` — user dashboard page after login